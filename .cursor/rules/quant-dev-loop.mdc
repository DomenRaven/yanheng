---
description: 本项目(stock-quant-system)每个开发任务必须遵循的闭环工作流——读文档对齐→定方案→实现→测试打分→验收→留痕→清理→微调下一个任务，禁止跳步
alwaysApply: true
---

# 量化研发任务闭环工作流

参考并融合了行业实践：ThoughtWorks CD4ML（代码+数据+模型三位一体版本化，新模型必须用同一套协议
跑赢当前"冠军模型"才能替换）、CRISP-DM 迭代循环、对冲基金 MLOps 的"结论必须是可复现证据包"原则、
本项目自身理论库 `docs/03-量化方法/10-时序验证与标签工程.md` 的 Purged K-Fold/Walk-Forward 纪律。
本仓库 Phase 0~1 的验收报告（`stock-quant-system/docs/phase0-acceptance-report.md`、
`phase1-acceptance-report.md`）是这套流程已经跑通的真实样板，新任务参照其格式。

## 每个任务必须依次走完 8 个闸门，禁止跳步

1. **读文档对齐需求**：先读顶层计划文档对应 Phase 条款 + `docs/03-量化方法/` 相关理论篇 + 已有验收
   报告，确认不会和已有结论矛盾或重复实现。禁止凭记忆/猜测直接开工。
2. **定框架和技术栈**：优先复用项目里已有模块（`research/`、`common/`、`ingestion/`），确认没有可
   复用实现才新建文件。非平凡的技术选型（模型结构、验证协议、数据源取舍）要写一句"为什么选这个、
   放弃了什么"，作为轻量版 ADR 留在验收报告或代码注释里。
3. **开工实现**：小步推进，一次聚焦一个模块/一个功能，不做跨多个 Phase 范围的一次性大改（纯清理
   任务除外）。
4. **测试与打分**：用真实数据跑出量化指标（IC/RankIC/回测收益/统计检验 p 值/覆盖率查询等），不是
   "跑起来不报错就算过"。不达标 → 回到步骤2/3小循环重做；禁止为了让数字好看而修改评估口径本身。
5. **验收**：对照计划文档该 Phase 的**验收条款原文**逐条核对，不满足的要么修到满足，要么在报告里
   写清楚"这是数据源上限/工程决策，理由是……"，不能假装通过。
6. **文档与留痕**：写/更新 `docs/phaseN-acceptance-report.md`，更新 README 与顶层 plan 文档的 todos
   状态；关键产出（模型/因子表/回测结果）要有版本化留痕（参照 `mlops/registry/<run_id>/metadata.json`
   的模式：记录数据范围、超参数、样本外指标）。
7. **清理**：任务做完立刻删掉本轮产生的一次性脚本/临时文件/`__pycache__`，不要攒到"以后统一清理"。
8. **微调后续任务**：若本轮真实结果和计划文档原始设想有出入（数据缺口、指标不如预期、优先级变化），
   在开工下一个任务前先调整任务清单和验收标准并写明理由，不要硬着头皮按旧计划做下去。

## 模型/架构升级的冠军-挑战者门禁（CD4ML）

新模型要替换已注册的基线（冠军）模型，必须用**同一套验证协议**（同样特征、同样 Walk-Forward 切分、
同样成本假设）跑出样本外指标，且显著优于当前冠军才能替换或融合；不显著则保留基线为生产模型，
如实记录对比结果——包括"新模型没有更好"这种结论，不能为了显得有进展而强行替换。

## 禁止事项

- 禁止跳过"测试与打分"直接判定验收通过
- 禁止把"看起来能跑"当作"验证通过"
- 禁止不清理就进入下一个任务，导致仓库堆积僵尸文件/临时脚本
- 禁止一次性修改超过一个 Phase 范围的内容
