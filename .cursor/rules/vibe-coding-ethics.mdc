---
description: Vibe coding八荣八耻——本项目所有开发工作的道德底线，实现前自查、验收报告里如实体现
alwaysApply: true
---

# Vibe Coding 八荣八耻（本项目道德约束）

以下八条是本项目所有开发工作的强制底线，优先级高于"完成速度"。每次实现前自查，事后在验收报告里
如实体现（尤其是第7条——不确定的地方要写出来，不要装作确定）。

1. **以臆猜接口为耻，以查档求证为荣**
   调用任何第三方库（akshare/tushare/lightgbm等）接口前，先读官方文档/源码签名或用小样本真实请求
   验证返回结构，不凭记忆猜字段名。本项目已有多次"猜接口返回结构"导致的 KeyError（见
   `docs/phase0-acceptance-report.md` 缺口2），是前车之鉴。

2. **以模糊开工为耻，以对齐需求为荣**
   开工前必须能一句话说清楚"这个任务对照计划文档哪一条、验收标准是什么数字"，说不清楚就先去读文档，
   不能"大概理解了就开始写"。

3. **以脑补业务为耻，以请示规则为荣**
   A股规则（涨跌停幅度、ST规则、T+1、除权除息）以 `research/a_share_rules.py` 或官方数据表
   （`limit_price`/`suspend_calendar`）为准，不凭印象假设细节。金融/统计方法（三重障碍、
   Purged K-Fold等）对照 `docs/03-量化方法/` 理论篇实现，不自己发明"看起来差不多"的变体。

4. **以新增冗余为耻，以复用存量为荣**
   写新功能前先搜索项目里是否已有同类实现（`common/`、`research/`、`ingestion/`），能扩展已有函数
   就不新建文件；发现真正废弃的代码要删除，不留着"以防以后用得上"。

5. **以省略校验为耻，以完备测例为荣**
   任何量化结论（因子有效、模型有提升、缺口已修复）必须有可复现的真实查询/统计检验支撑
   （t检验/p值/覆盖率查询等），不能只放一段没验证过的代码或一句"应该没问题"。

6. **以乱改架构为耻，以恪守规范为荣**
   遵循已确立的项目结构（EAV财务表、DuckDB单写者、`research/`纯函数 vs `panel.py`唯一摸库模块的
   分层约定等），改动跨越既有边界前先说明原因，不随手打破分层。

7. **以不懂装懂为耻，以坦诚存疑为荣**
   遇到数据质量问题、统计显著性不够、方法论边界，必须在文档里如实写出来（参考
   `docs/phase1-acceptance-report.md` 第6节"已知局限"的写法），不能为了报告好看而回避。

8. **以批量乱改为耻，以分步迭代为荣**
   一次改动聚焦一个任务/一个模块，改完测完再进入下一个，不做"一口气改十个文件、最后再一起排查是哪个
   出错了"式的批量操作。
